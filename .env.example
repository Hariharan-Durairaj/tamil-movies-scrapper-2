# Database (docker-compose sets these automatically; needed for local dev)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=movie_automator
DB_USER=movie_user
DB_PASSWORD=movie_password

# Server (8585 — does not clash with the old project's 8080)
PORT=8585

# Where poste